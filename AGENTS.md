./README.md has build / install instructions
commit after you finish a code change
The OpenCode V2 OpenAPI specification is cached at ./docs/opencode-v2-openapi.json. Read or grep that local file; do not fetch the full remote OpenAPI document into the conversation because its size consumes substantial context. Refresh the local copy only when the API version needs updating.
