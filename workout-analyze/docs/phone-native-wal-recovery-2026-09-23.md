# Native DuckDB WAL relaunch investigation (September 23, 2026)

Fresh device `Library/Application Support` capture before this fix: `/Users/kkrausse/Documents/repos/kkrausse/random/workout-analyze/data/phone-recovery-2026-09-23-wal-investigation/Application Support/`. This ignored directory contains the original, native, and both recovery DuckDB files and their WALs, as well as recorder SQLite, WAL, and SHM. Never open any captured database in place with a writable DuckDB host. The diagnostic log records that at 14:43:14Z pinned DuckDB Swift 1.1.3 failed to replay the first recovery WAL during commit at `art.cpp:735: depth < key.get().len` and selected a newly created recovery file instead.

On **disposable database+WAL copies** opened by Bun DuckDB 1.5.5, `analysis-native.duckdb` contains 173 workouts, 195,928 samples, 31 routes, 1,477 traversals; the first recovery (`analysis-recovery-2026-09-23T14-29-44Z-cb172a65-a44a-40cb-a0c9-c3c02042e65e.duckdb`) contains 173 workouts, 195,928 samples, 31 routes, **421 traversals**. Its WAL SHA-256 in this capture is `d129548514614eea2eed1a8c5121ec161f7cbf0b0cf4dbcb993c5d2c386799cd`. The later recovery (`...14-43-14Z-2ebba983-bc76-45c5-9b27-c1fdd5c21c20.duckdb`) has three recorder workouts and 2,719 samples, with no route tables. The populated recovery **is salvageable on DuckDB 1.5.5**; opening that exact WAL in pinned native 1.1.3 remains blocked. Do not discard its WAL or replace the phone's store with a database checkpointed by another major engine version without native compatibility verification.

Root cause is the 1.1.3 ART-index WAL replay assertion on a committed, indexed analysis dataset. The previous startup selector allowed that failure to masquerade as an empty successful store on the next launch. The native fix checkpoints after completed transactions and standalone writes (including route publication and import) and on background rollback, and fails closed at the oldest recovery store instead of choosing a later empty one. A 1.1.3 arm64 simulator test wrote 173 workouts, 195,928 samples, 31 routes, 421 traversals, and an indexed traversal table across transactions, asserted that checkpoint left no WAL, and reopened with the same counts. The entire `DuckDBServiceTests` suite passed: 11 tests, zero failures.

The checkpoint fix alone prevents future uncheckpointed successful writes, but **does not repair** the already-stranded phone WAL. The validated migration below preserves the WAL and activates a separately imported native-format database instead.

## Populated-state migration and physical-device result

The first recovery database **and WAL** were copied to `/private/var/folders/t_/x48jtnps7n5_0g_pt9xpvbg00000gn/T/workout-wal-compat/source.duckdb[.wal]`; Bun DuckDB 1.5.5 replayed this disposable copy and `EXPORT DATABASE ... (FORMAT PARQUET)` produced eight tables, schema/index SQL, and data files in that temporary directory's `export/`. Swift DuckDB **1.1.3** then executed `IMPORT DATABASE` into a *new* database on the arm64 simulator. The imported native database checkpointed without a WAL, reopened through `DuckDBService.applicationDatabase`, and passed exact counts (173 workouts, 195,928 samples, 31 routes, 421 traversals, 1,890 coverages) and identity digests: route IDs/card counts `e47d85d7e98e93bad1fd63112582dd8f`, traversal IDs/route/workout association `66ab2962eac0f0b059a6e5fccd3da2cd`, workout IDs `6d99f0b4cb2542f2b774d865e7c73fef`. This is a native-1.1.3-written file, **not** a 1.5.5 checkpoint copied to iOS. Simulator tests also passed selection after a new write/reopen and fail-closed rejection of a changed activation marker. The 12-test `DuckDBServiceTests` suite passed (one optional captured-export integration test skipped in the final generic suite); the captured-export test passed separately with the real fixture.

Fresh untouched pre-install phone capture: `data/phone-recovery-2026-09-23-pre-migration/Application Support/`. The first recovery WAL still hashes `d129548514614eea2eed1a8c5121ec161f7cbf0b0cf4dbcb993c5d2c386799cd`. The new native artifact was staged under a separate filename `analysis-restored-2026-09-23.duckdb`, SHA-256 `f43cce6cfcbf75a864df39d75f751d95ae7725e97a1f2efa6247c296b08eff4c`, copied to the iPhone and read back with the identical hash. The activation marker `analysis-recovery-selection.json` names only that artifact and the source WAL fingerprint. Native startup verifies the artifact hash, no WAL, counts, and all three identity digests before creating `analysis-recovery-activated.json` as an atomic receipt and selecting it. Subsequent launches accept the unchanged receipt/marker pair so ordinary writes can change the database hash. A missing/corrupt/mismatched marker or an unreplayable restored file fails closed and reports diagnostics; no fallback overwrites original stores. To roll back the selection, remove or rename **only** the marker and receipt while the app is stopped; the old first recovery remains present and will fail closed until another recovery is selected. Preserve the restored file for any later investigation.

The signed native build was installed on the physical iPhone. Via the development page's read-only `database.query` diagnostics, first launch and a terminated/relaunched app both returned 173/195928/31/421/1890. Route and traversal digests matched; the supported `app.actions.loadRoutes()` and `loadLibrary()` workflows returned 31 route cards (including `loop-053e9f632ad0b46bfe16`, 200 traversals/23 workouts) and 173 workouts, with analysis settings present. `analysisHostAvailable` was true. The post-relaunch phone capture is `data/phone-recovery-2026-09-23-post-migration/Application Support/`. Comparing SHA-256 against the pre-install capture, **all eight old DuckDB database/WAL files**, recorder SQLite database, and recorder SQLite WAL remained byte-identical. The SQLite SHM changed as normal on launch. No Mac reimport or analysis rebuild was performed; the restored first analysis still has its original route cards.
