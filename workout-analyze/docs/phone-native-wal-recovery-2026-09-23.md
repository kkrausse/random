# Native DuckDB WAL relaunch investigation (September 23, 2026)

Fresh device `Library/Application Support` capture before this fix: `/Users/kkrausse/Documents/repos/kkrausse/random/workout-analyze/data/phone-recovery-2026-09-23-wal-investigation/Application Support/`. This ignored directory contains the original, native, and both recovery DuckDB files and their WALs, as well as recorder SQLite, WAL, and SHM. Never open any captured database in place with a writable DuckDB host. The diagnostic log records that at 14:43:14Z pinned DuckDB Swift 1.1.3 failed to replay the first recovery WAL during commit at `art.cpp:735: depth < key.get().len` and selected a newly created recovery file instead.

On **disposable database+WAL copies** opened by Bun DuckDB 1.5.5, `analysis-native.duckdb` contains 173 workouts, 195,928 samples, 31 routes, 1,477 traversals; the first recovery (`analysis-recovery-2026-09-23T14-29-44Z-cb172a65-a44a-40cb-a0c9-c3c02042e65e.duckdb`) contains 173 workouts, 195,928 samples, 31 routes, **421 traversals**. Its WAL SHA-256 in this capture is `d129548514614eea2eed1a8c5121ec161f7cbf0b0cf4dbcb993c5d2c386799cd`. The later recovery (`...14-43-14Z-2ebba983-bc76-45c5-9b27-c1fdd5c21c20.duckdb`) has three recorder workouts and 2,719 samples, with no route tables. The populated recovery **is salvageable on DuckDB 1.5.5**; opening that exact WAL in pinned native 1.1.3 remains blocked. Do not discard its WAL or replace the phone's store with a database checkpointed by another major engine version without native compatibility verification.

Root cause is the 1.1.3 ART-index WAL replay assertion on a committed, indexed analysis dataset. The previous startup selector allowed that failure to masquerade as an empty successful store on the next launch. The native fix checkpoints after completed transactions and standalone writes (including route publication and import) and on background rollback, and fails closed at the oldest recovery store instead of choosing a later empty one. A 1.1.3 arm64 simulator test wrote 173 workouts, 195,928 samples, 31 routes, 421 traversals, and an indexed traversal table across transactions, asserted that checkpoint left no WAL, and reopened with the same counts. The entire `DuckDBServiceTests` suite passed: 11 tests, zero failures.

The fix prevents future uncheckpointed successful writes, but **does not repair** the already-stranded phone WAL. A separately validated migration or compatible DuckDB upgrade is required to make the populated recovery available on the phone; do not trigger another phone import or rebuild to conceal this state. The updated native build has not been installed on the device, and no on-phone relaunch persistence test has passed yet.
