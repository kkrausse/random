# Palma2 reconnect and project review — September 15, 2026

All times are PDT (UTC−07:00). This is a sanitized gateway review, with historical
ADB evidence explicitly distinguished from current observations.

## Latest reconnect

The Pi clock read 08:20:13 during the initial check. One authenticated station
had an association age of 102 seconds, consistent with reconnecting at about
08:18:31. AP and capture services were active; capture has run since September
10 at 21:39:54. The ring contained approximately 49 MB, in its first file.

The final extracted window contains **920 device-associated packets from
08:18:30.635 through 08:21:36.770**, read at 08:21:55. Captured frame bytes below
include handshakes, headers, and retransmissions; they are not payload sizes.

| Public destination group | Outbound | Inbound | Interpretation |
| --- | ---: | ---: | --- |
| Tailscale TLS/control/relay | 85,983 B | 197,173 B | Control-plane connections and 28 distinct named relay hosts; consistent with relay selection |
| Tailscale STUN | 410 B | 370 B | Five binding request/response exchanges; peers matched TLS names or the current official DERP map |
| Google `www.google.com` | 3,014 B | 11,083 B | Paired TLS connections consistent with the replacement validation endpoint |
| Google `mtalk.google.com`, TCP 5228 | 2,308 B | 11,199 B | Google messaging connection; originating UID not established |

- No other public peers appeared in this window. No visible BOOX DNS/TLS names
  or connections to historical BOOX addresses appeared.
- Three DNS queries requested `sync.datamate.kindle.amazon.dev` (A/AAAA at
  reconnect and another A query at 08:19:35). Replies returned addresses, but
  no subsequent direct connection to those addresses appeared. This is evidence
  of DNS metadata exposure, not a successful Kindle sync or app attribution.
- Local IPv4 activity was DHCP, DNS, and six TCP SYN attempts/retransmissions
  toward the Pi resolver's port 853, without a SYN-ACK. ARP and Wi-Fi handshake
  traffic were also present.
- Eight device-originated IPv6 packets were link-local multicast membership,
  router solicitation, and duplicate-address detection. No global IPv6 traffic
  was observed. The Pi has no IPv6 address on wlan0 and retains IPv6 guard
  denials. Seeing a link-local attempt is not evidence of IPv6 Internet access.
- No LAN/container connection attempts or broad scanning pattern appeared in
  the latest window.

## VPN and isolation assessment

The live `inet palma_guard` table was inspected. It denies AP forwarding to
private, link-local, shared-address, and multicast/reserved IPv4 ranges; denies
non-Ethernet egress and IPv6; and restricts Pi input to DHCP/DNS. Current LAN
and Docker routes fall within the denied ranges. Counters include historical
drops but cannot date or attribute individual attempts to this reconnect.

Current traffic is consistent with the last verified policy, but **there was
no attached ADB device for fresh device-side verification**. September 11 ADB
evidence established Tailscale always-on, lockdown enabled, reader/Gmail/
1Password UID inclusion, Chrome/Kindle exclusion, and no exit-node default
route. Prior user tests found Chrome and Kindle offline.

**App restrictions are not full-tunnel privacy.** Without an exit node, direct
public traffic is expected and is not automatically a VPN bypass. The system
also previously made Android validation probes under lockdown. Current Google
validation-like traffic and local Kindle-related DNS show why the policy must
not be described as blocking every excluded-app-related or system packet.
Encrypted Tailscale contents and current app settings are not visible here.

The Pi guard controls the outer network path. An authorized connection carried
inside an encrypted Tailscale relay can still reach a permitted tailnet peer;
AP isolation does not override tailnet permissions or inspect inner traffic.

## Review since the previous report

A MAC-filtered extraction of the live ring covered September 10 at 21:40:07
through September 15 at 08:19:45 (68,710 packets). After the prior September 11
22:31:10 cutoff, 1,154 packets appeared in that extraction, with public peers
limited to Google and Tailscale. No device-MAC packets occurred on September
12 or 13. Silence is not proof of enforcement or evidence about other Wi-Fi
networks. A reconnect burst also appeared September 14 at 20:52.

## Project history and privacy findings

1. **Isolated Pi AP built.** NetworkManager replaced competing Ethernet
   management by dhcpcd, with a guarded reboot. Dedicated Wi-Fi, NAT, scoped
   firewall denials, and bounded continuous capture were installed. Namespace
   firewall tests, UFW reload, and AP restart passed. Physical-client isolation
   and boot/service-restart acceptance remain incomplete; automatic AP startup
   is disabled. Keep the single-client operating assumption.
2. **September 10 passive baseline, 21:22:32–21:39:40.** Archived capture stopped
   with 18,968 received packets and zero reported drops. This review extracted
   18,916 device-associated packets from that archive. BOOX, Google, Amazon/
   Kindle, Audible, and Substack were contacted. Large transfers were primarily
   downloads, including Chrome optimization data and Kindle/media content.
3. **RethinkDNS trial.** Some apps were subsequently allowed, so the entire trial
   was not an all-apps-blocked test. Amazon event/metrics and Kindle diagnostics
   continued while Kindle was allowed. RethinkDNS occupied Android's VPN slot,
   complicating simultaneous Tailscale access for the custom reader.
4. **Tailscale inclusion policy.** Tailscale infrastructure appeared around
   September 10 at 23:13. The final reported inclusion list was reader, Gmail,
   and 1Password. Chrome and Kindle were excluded, with prior user tests and
   September 11 ADB evidence supporting enforcement. Exact change times vary
   in certainty; earlier Kindle traffic is not proof of a later-policy failure.
5. **BOOX connectivity checks explained and changed.** September 11 ADB logs
   matched the suspicious small wake bursts to Android NetworkMonitor requests
   for `en-ntp.boox.com/generate_204`, returning HTTP 204. At 07:36 the user
   requested replacing BOOX validation URLs with Google. Subsequent logs and
   captures verified Google checks. Current metadata remains consistent with
   that change; reboot persistence has not been directly tested.

### What was privacy-relevant before?

- **BOOX:** `en-data.boox.com`, `push.boox.com`, `index1.boox.com`, and
  `cb.boox.com` had encrypted baseline connections. Across those four named
  stream groups, approximately 65 KB outbound and 161 KB inbound frame bytes
  were captured. Their names do not establish payload contents. Separate
  `en-ntp`/`eu-ntp` plaintext `/generate_204` requests and later ADB-correlated
  checks establish connectivity validation, not document uploading.
- **Amazon:** `unagi-na.amazon.com` / `unagi.amazon.com` event/metrics endpoints,
  Kindle Bugsnag session/trace endpoints, and a Minerva-named device endpoint
  appeared alongside normal sync, delivery, and messaging. The earlier trial
  review measured approximately 428 KB outbound to the two Unagi groups while
  Kindle was allowed. Diagnostic/metrics collection is a genuine privacy
  consideration, but exact events, identifiers, and reading data are unknown.
- **Google:** baseline connections included `locationhistory-pa.googleapis.com`,
  `userlocation.googleapis.com`, `footprints-pa.googleapis.com`, Firebase logging/
  installations, and `ogads-pa.googleapis.com`, alongside account, Play, sync,
  messaging, and Chrome services. Location-related endpoint contact does not
  prove transmission of GPS coordinates or enabled Location History. Some
  transfers can be account/configuration requests or failures; TLS hides them.
- **Tailscale:** earlier reviews measured approximately 1.98 MB outbound frame
  bytes to its logging endpoint overnight and approximately 1.03 MB in a later
  daytime window. Contents were encrypted. Relay/STUN traffic exposes connection
  timing and source public address to infrastructure; STUN responses explicitly
  reported the NAT-mapped public address. Relay traffic does not by itself show
  that Tailscale can read the inner application data.
- Every directly contacted server necessarily sees a connecting public IP and
  timing. Plaintext DNS exposes requested names to the local resolver. We have
  not established uploads of documents, credentials, precise location, or a
  local-network inventory, nor established that all encrypted traffic is benign.

### Local-network activity: fuller historical review

The baseline contained six IPv6 mDNS queries for `_meshcop._udp.local`, a
Thread border-router discovery service, as well as normal IPv6 setup traffic.
This is actual local service discovery. It is not a broad address/port scan,
and no returned device inventory or subsequent inventory upload was established.
Baseline outbound private IPv4 traffic was directed to the AP gateway or DHCP
broadcast, primarily DNS and unsuccessful DNS-over-TLS attempts.

The later ring contains repeated UDP/41641 attempts to the Pi's AP, LAN, and
container addresses. Those destinations match the Pi's interface inventory
and Tailscale's usual peer-connectivity port, consistent with advertised peer
candidate testing. It also contains DNS-over-TLS attempts to private/shared
resolver addresses. These are attempts visible before forwarding decisions,
not proof of successful LAN access. The live guard denies these paths; this
review did not perform a simultaneous uplink capture or an end-to-end physical
client isolation test. No broader port/address-scanning pattern was found in
the extracted metadata. Passive listening cannot be ruled in or out.

## Evidence and remaining uncertainty

Private, Git-ignored metadata is in:

- `private/review-20260915-baseline.tsv`
- `private/review-20260915-metadata.tsv`
- `private/review-20260915-latest.tsv`

The files were parsed successfully with tshark; the live file was read while
recording continued, so the exact extraction cutoff differs by snapshot.
Final live-run loss statistics remain unavailable until capture stops. No
HTTPS interception was used. Capture remains running. No device/network policy
was changed for this review. Fresh USB ADB inspection plus timestamped excluded-
app tests after reboot/wake would provide stronger current enforcement evidence.
